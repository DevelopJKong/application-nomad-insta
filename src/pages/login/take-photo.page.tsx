import { View, Text } from 'react-native';
import React from 'react';

const TakePhoto = () => {
   return (
      <View>
         <Text>TakePhoto</Text>
      </View>
   );
};

export default TakePhoto;
